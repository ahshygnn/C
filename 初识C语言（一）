#include<stdio.h>
//int main()//主函数-程序的入口。不能没有main函数，也不能有多个main函数；int为整型，main函数前的int表示main函数调用返回一个整型值
//{
//	printf("hehe\n");
//	//这里完成任务
//	//在屏幕上输出
//	//库函数——打印函数
//	return 0;//返回整数0
//}

//变量类型
// char字符型1字节8个比特位；short短整型2字节16个比特位；int整形4字节32个比特位；long长整型4/8字节32/64比特位；long long更长整型8字节64比特位；float 单精度浮点数4字节32比特位；double 双精度浮点数8字节64比特位
// %d打印整型
// %c打印字符
// %f打印浮点数字-打小数（5位小数）
// %lf打印双精度浮点型（6位小数）
// %p以地址的形式打印
// %x打印16进制数字
int main()
{
	printf("%d\n", sizeof(char));
	printf("%d\n", sizeof(short));
	printf("%d\n", sizeof(int));
	printf("%d\n", sizeof(long));
	printf("%d\n", sizeof(long long));
	printf("%d\n", sizeof(float));
	printf("%d\n", sizeof(double));
	return 0;
}
//1
//2
//4
//4
//8
//4
//8
//字节byte，比特位bit 计算机中的单位 bit-byte-kb-mb-gb-tb-pb
//计算机是硬件--通电；正电1；负电0，
//一个比特位存放一个二进制位
//一个字节=8个比特位大小=8个二进制位
//1kb=1024byte
//1mb=1024kb 兆B
//其他的都是1024
//n个比特位的排列组合可能性为2的n次方种，如8个比特位（1个字节）的可能性为2^8；
//二进制算法：010 0*2^2+1*2^1+0*2^0
//int整型4个字节32个比特位能表达2^32种状态，能够表达的数字范围为0——2^32-1
//恰当使用变量类型能够节省空间,比如能用short int的时候不用int就可以节省一半的空间
